"""Notifications application package.

This app implements a simple notification system using the
Observer pattern.  Notifications are created when certain events
occur (e.g. a recruiter accepts or rejects a resume) and are
persisted in the database for later display to the user.
"""
